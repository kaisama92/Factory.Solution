@{
  Layout = "_Layout";
}

@model Factory.Models.Machine

<h1>You are about to Remove @Html.ActionLink($"{Model.MachineName}", "Details", new { id = Model.MachineId })!</h1>
<h1>Are you sure you want to continue?</h1>

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}

<p>@Html.ActionLink("Cancel", "Details", new { id = Model.MachineId })</p>